version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
# FRONTEND TEMPORARILY DISABLED FOR BACKEND-ONLY DEPLOYMENT
# This branch is dedicated to backend infrastructure deployment only
# Includes: PostgreSQL Aurora, Lambda functions, GraphQL API, VPC, IAM roles
# Frontend deployment will be re-enabled after backend validation
# frontend:
#   phases:
#     build:
#       commands:
#         - npm run build
#   artifacts:
#     baseDirectory: dist
#     files:
#       - '**/*'
#   cache:
#     paths:
#       - .npm/**/*
#       - node_modules/**/*
