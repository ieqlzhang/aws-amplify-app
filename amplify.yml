version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
# Frontend deployment disabled for backend-only testing
# Uncomment the frontend section below when ready to deploy the React frontend
# frontend:
#   phases:
#     build:
#       commands:
#         - npm run build
#   artifacts:
#     baseDirectory: dist
#     files:
#       - '**/*'
#   cache:
#     paths:
#       - .npm/**/*
#       - node_modules/**/*
